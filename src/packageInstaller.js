module.exports = {

    packageInstaller: function(pkgs){
        // validate input

        if (typeof pkgs !== 'array') {
            return 'err: an array is required input'
        }

        let installOrder = ''

        return installOrder; 
    }

}